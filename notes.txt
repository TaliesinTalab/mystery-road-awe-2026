# Notes

## Steps
cloned repo, start server via
    python -m http.server 8080
opened devtools
Reloaded page, nothing weird visible. Console says:
    "First note preview: Promise"
Clicking on "Promise" shows
    "Prototype: Promise" "PromiseState: fulfilled" "PromiseResult: """

Clicked on 'Evidence' button for navigation, shows this error in console:
    app.js:1040 Uncaught TypeError: Cannot read properties of undefined (reading 'getAttribute')
        at HTMLButtonElement.<anonymous> (app.js:1040:38)
    (anonymous)	@	app.js:1040

Same error as above when clicking on 'Workspace'.

Clicking 'People & Locations' is fine. 'Timeline shows a new error':
    VM14:2 Uncaught TypeError: Cannot read properties of undefined (reading 'startTime')
        at et.reportAllChanges (<anonymous>:2:19429)
        at <anonymous>:2:13070
        at <anonymous>:2:331
        at d (<anonymous>:2:6141)
        at u (<anonymous>:2:6153)
        at <anonymous>:2:6321
        at <anonymous>:2:2895
        at n.timeout (<anonymous>:2:5652)

    Turns out this is a vivaldi thing

Clicking on 'Dashboard' when elsewhere shows the same error as above.

Evidence catalogue (in Evidence tab) loads forever.
When in 'People & Locations', there are a total six entries, each with a disgusting AI generated picture of a professor.
Clicking on 'View', brings me to 'Evidence' with the same problems as before.
Back to 'People & Locations', going to the 'Locations' subtab shows six entries.

Now on 'Timeline', clicking on 'View E15' does two things;
    console output: modal opened, active close listeners: 1
    opens pop-up window, clicking on view evidence redirects to 'Evidence', where:
        shows actual evidence report, but above the report is the infinitely loading remainder of the evidence page.
        going to dashboard and then back to evidence does not remove the report

    I just noticed that under 'Location:' it says [object Object]

Workspace: couldn't find a way to bookmark earlier.
    After sending a hypothesis, in the DevTools under Application (localhost 8080), I found this:
        Key: remotion_hypothesis
        Value: {"suspectId":"signal-scholar","nature":"accidental","evidenceIds":["E14","E16","E17"],"confidence":"67","explanation":"w35","alternative":"w34","savedAt":"2026-09-09T18:31:07.738Z"}

Note for report: I couldn't bookmark because the evidence list never loaded (one bug hiding another)
To lift this temporarily, I went to Evidence and typed in:
    evidenceViewLoading = false; renderEvidenceList();
This then showed me the evidence entries.


***

Changes:

1.
In index.html:
- <script src="app.js"></script>
+ <script type="module" src="app.js"></script>
(simply added type="module")

After reloading, I cannot navigate anymore:
	VM342675 :1 Uncaught ReferenceError: navigateTo is not defined
    at HTMLButtonElement.onclick (VM342675 :1:1)
onclick	@	VM342675 :1
	Uncaught TypeError: Cannot read properties of undefined (reading 'getAttribute')
    at HTMLButtonElement.<anonymous> (app.js:1040:38)
(anonymous)	@	app.js:1040

The second one is old. The first is new, why did navigateTo break ? Error says it's undefined but it's written at line 182.
In the DevTools console:
	navigateTo
		VM342702:1 Uncaught ReferenceError: navigateTo is not defined
    		at <anonymous>:1:1
	typeof navigateTo
		'undefined'
	window.navigateTo
		undefined

To test the differences, I undid my change to index.html, and redid the same commands:
	navigateTo
		ƒ navigateTo(viewName) {
  			window.location.hash = viewName;
			 // handleHashChange() will pick this up via the hashchange listener
		}

	typeof navigateTo
		'function'

	window.navigateTo
	(same as navigateTo)

Further experimentation: redid the index.html, and then while at a breakpoint at line 1084 in the app, the commands gave me two different results:
	navigateTo
		ƒ navigateTo(viewName) {
  			window.location.hash = viewName;
			 // handleHashChange() will pick this up via the hashchange listener
		}
	window.navigateTo
		undefined

--> What does this mean ? Well, I asked it basically run the same function in two different ways. First was to just run the navigateTo function, assuming it knows it (as if it were a global function), and the other asking it to run the window's navigateTo function (as if it were a function available to the window class). How come the first is different from the when not paused ? Is it the hashchange at 1085 that causes this difference between the two ?
	It can't be that, since that function only adds a listener, nothing can be lost there.


Correction to the above point: Instead of running functions in different ways, I asked it if those 'names' mean anything to it. In that sense, it only recognises navigateTo as a function, but does not recognise window.navigateTo


--> continuing the app and then attempting to navigateTo makes it return an error, saying that it is not defined
--> when the suite finished building, the function is lost ? False
	Running location.hash = "timeline" navigates to the timeline, meaning that the hash listener is alive ergo it shouldn't be the 
	build that lost anything.


Line 28 of index.html:
      <button type="button" class="nav-btn" data-view="dashboard" onclick="navigateTo('dashboard')">Dashboard</button>

function outer() { function inner() { return "hi"; } return typeof inner; }
	undefined
outer()
	'function'
typeof inner
	'undefined'
What is happening here ? inner is nested inside outer, so it is not being detected, I think. If I were to apply this to the earlier problem: It is nested so cannot be reached ?

	Result: Yes, it is an issue of nesting. Originally, without type="module", all functions were at the top level, but now they are all nested inside. That's why window. could not reach them. 
While paused at 1084, I can add a reference from within the room so that the outside can see the function. This is done by typing
	window.navigateTo = navigateTo
Then window.navigateTo is recognised and identical to navigateTo.
Now, this is not the only victim, in index.html, I also found the following victim functions: switchPeopleTab, saveHypothesis, handleSortChange
In app.js, I also found the victims closeEvidenceDetail and saveCurrentNote
In app.js, by searching for setAttribute( :renderEvidenceList

Compiling them together, I need to add references to the outside from app.js:
// ---------------------------------------------------------------------
// GLOBALS REQUIRED BY THE INLINE HANDLERS IN index.html
// ---------------------------------------------------------------------

window.navigateTo = navigateTo;
window.switchPeopleTab = switchPeopleTab;
window.saveHypothesis = saveHypothesis;
window.handleSortChange = handleSortChange;
window.closeEvidenceDetail = closeEvidenceDetail;
window.saveCurrentNote = saveCurrentNote;
window.renderEvidenceList = renderEvidenceList;


***

Now for the extraction to utils.js
The following functions qualify:
evidenceMentionsPerson
formatDate
getStatusBadgeClass
getRelevanceBadgeClass
certaintyBadgeClass
getSelectedOptions


***
navigateTo is a function I originally wanted to port to utils.js
It is called by three functions within app.js:
renderPeople() (which then is called by an else if block by handleHashChange, along with 5 other functions that do not call navTo)
openEvidenceModal()
renderBookmarksList()

Proposition: file containing hash and its six called files as well as navTo. No longer a loop, right ?
